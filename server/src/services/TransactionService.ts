import { prisma } from "../lib/database/prisma";

class TransactionService {
    async deposit(pin: string, ATMid: string, iban: string, amount: number, description: string) {
        
    }
}

export default TransactionService;